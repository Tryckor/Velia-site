import Home from "@/app/page";

// Design version — warm "paper" canvas (cream background, warm accent) instead
// of stark white, for a softer editorial feel.
export default function PaperVersion() {
  return (
    <div className="theme-paper">
      <Home />
    </div>
  );
}
